node_modules
dist
.DS_Store
.idea/
server/public
vite.config.ts.*
*.tar.gz
*.db
*.db-shm
*.db-wal
.env
.env.local

# Cloud SQL proxy binary (download separately — never commit binaries)
cloud-sql-proxy
cloud-sql-proxy.exe

# Firebase local state (project ID is user-specific — set via `firebase use PROJECT_ID`)
.firebaserc

# Debug / scratch files
git_diff.txt
*.diff.txt

# One-off helper scripts (use npm run scripts instead)
grant.js
grant.cjs
test.cjs

# Capacitor build artifacts (but keep the project files)
android/app/build/
android/.gradle/
android/build/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/capacitor-cordova-android-plugins/build/
ios/App/App/public/
ios/App/App/capacitor.config.json
ios/App/Pods/
*.apk
*.aab
*.ipa
*.keystore
*.jks

# Tauri
src-tauri/target/

# Test coverage output
coverage/
