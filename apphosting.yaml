# Settings for Backend (on Cloud Run).
runConfig:
  minInstances: 0

env:
  - variable: SESSION_SECRET
    secret: SESSION_SECRET
    availability:
      - RUNTIME

  - variable: DATABASE_URL
    secret: DATABASE_URL
    availability:
      - RUNTIME
